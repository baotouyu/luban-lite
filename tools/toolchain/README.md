# Toolchain Archives

The upstream Luban-Lite v1.3.0 SDK ships these toolchain archives in `tools/toolchain/`:

- `Xuantie-900-gcc-elf-newlib-mingw-V2.6.1-20220906-trim.tar.gz`
- `Xuantie-900-gcc-elf-newlib-x86_64-V2.6.1-20220906-trim.tar.gz`

They are larger than GitHub's regular Git blob limit, so this GitHub-maintained SDK copy stores them as release assets instead of committing them to Git history.

Download them from the `luban-lite-v1.3.0-toolchains` release and place them back in this directory when needed.
